"""AI-agent tooling: an MCP server exposing Zeython project introspection
and documentation search. Requires the `mcp` extra (`pip install
zeython[mcp]`) -- nothing in this subpackage is imported by the framework
core, so installs without the extra are unaffected.
"""
